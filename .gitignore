# OS
.DS_Store
Thumbs.db

# Raw scraped template source (mirrored, not original work) — lives on dev
/template/

# Internal business docs (not for public repo) — lives on docs branch
/1. Client Facing/
/2. Developer Facing/
/3. Market Research & Strategy/

# Editor/tooling
.claude/settings.local.json

# Real SMTP credentials — upload directly to the server, never commit
mail-config.php
